<% content_for :head do %>
	<%= stylesheet_link_tag 'prizes' %>
<% end %>

<table class="prizes_list">
<tbody>
  <tr><th colspan=4>Your Sponsored Prizes</th></tr>
 <tr><th>Title</th><th>Status</th><th colspan=2>Menu</th></tr>
<% for prize in @prizes %>
<tr><td class="name"><%=prize.name%></td>
 	<td class="status">Status: Active</td>
	<td class="command"><%= link_to 'Show', prize %></td>
    <td class="command"><%= link_to 'Edit', edit_prize_path(prize) %></td>
    <td class="command"><%= link_to 'Destroy', prize, :confirm => 'Are you sure?', :method => :delete %></td>
</tr>
<% end %>

</tbody>
</table>
<br />

<!-- <%= link_to 'Previous Page', {:page => @prize_pages.current.previous} if @prize_pages.current.previous %> -->
<!-- <%= link_to 'Next Page', {:page => @prize_pages.current.next} if @prize_pages.current.next %> -->

<%= link_to (image_tag("createaprize.gif", :border => 0)), new_prize_path %>
